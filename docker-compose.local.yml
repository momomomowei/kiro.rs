services:
  kiro-rs:
    build:
      context: .
      dockerfile: Dockerfile
    image: kiro-rs:local
    container_name: kiro-rs
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "8990:8990"
    volumes:
      - ./data/:/app/config/
    restart: unless-stopped
